{
  "name": "finance-backend",
  "version": "1.0.0",
  "description": "Finance Tracker API",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "migrate:categories:dry": "node scripts/migrate-full-categories.js --dry",
    "migrate:categories:apply": "node scripts/migrate-full-categories.js --apply"
},
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^7.6.0"
  },
  "engines": {
    "node": ">=18"
  }
}
